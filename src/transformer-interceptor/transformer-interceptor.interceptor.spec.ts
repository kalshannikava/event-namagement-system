import { TransformInterceptor } from './transformer-interceptor.interceptor';

describe('TransformerInterceptor', () => {
  it('should be defined', () => {
    expect(new TransformInterceptor()).toBeDefined();
  });
});
